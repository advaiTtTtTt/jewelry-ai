# Core ML / Deep Learning
# PyTorch is installed separately with CUDA support (see setup.sh)
# torch>=2.5.1
# torchvision>=0.20.1
# torchaudio>=2.5.1
# xformers==0.0.29.post3

# Zero123++ (multi-view generation)
diffusers>=0.30.0
transformers>=4.48.0,<5.0.0
accelerate>=0.24.0

# TripoSR (3D reconstruction) — cloned to models/TripoSR
# Dependencies:
# torchmcubes — NOT needed; scikit-image marching cubes fallback is used
einops>=0.7.0
omegaconf>=2.3.0
huggingface-hub>=0.20.0
scikit-image>=0.21.0

# 3D mesh processing
trimesh[easy]>=4.0.0
pygltflib>=1.16.0
numpy-stl>=3.0.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.11.0
rembg>=2.0.50

# Background removal network
onnxruntime>=1.16.0

# FastAPI backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0
pydantic>=2.5.0

# GroundingDINO — installed via pip: groundingdino-py
groundingdino-py>=0.4.0

# SAM2 — installed via pip: sam2
sam2>=1.0.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0
